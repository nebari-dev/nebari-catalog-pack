{
  "name": "nebari-catalog-pack-e2e",
  "private": true,
  "description": "Playwright screenshot harness for nebari-catalog-pack",
  "scripts": {
    "screenshots": "playwright test",
    "screenshots:live": "playwright test --config playwright.live.config.js",
    "install:browser": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
